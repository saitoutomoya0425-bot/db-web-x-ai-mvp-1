"use client";
import { useState } from "react";
export function XApiSettings({initial}:{initial:{query:string;enabled:boolean}}){const [query,setQuery]=useState(initial.query),[enabled,setEnabled]=useState(initial.enabled),[message,setMessage]=useState("");async function save(){setMessage("保存中…");const response=await fetch("/api/admin/integration-settings",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({query,enabled})});const data=await response.json();setMessage(response.ok?"保存しました":data.error)}return <section className="rounded-2xl border border-slate-800 bg-slate-900/40 p-6"><h2 className="font-bold">X収集条件</h2><textarea value={query} onChange={event=>setQuery(event.target.value)} rows={3} className="mt-4 w-full rounded-lg border border-slate-700 bg-slate-950 p-3"/><label className="mt-3 flex items-center gap-2 text-sm"><input type="checkbox" checked={enabled} onChange={event=>setEnabled(event.target.checked)}/>定期収集を有効化</label><button onClick={save} className="mt-4 rounded-lg bg-violet-600 px-4 py-2 text-sm">設定を保存</button>{message&&<span className="ml-3 text-sm text-slate-400">{message}</span>}</section>}
