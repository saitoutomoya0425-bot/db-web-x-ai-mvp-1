"use client";
import { useState } from "react";
type Settings={site_name:string;default_title:string;default_description:string;canonical_base_url:string|null;og_image_url:string|null;robots_index:boolean};
export function SeoSettingsForm({initial}:{initial:Settings}){const [value,setValue]=useState(initial),[message,setMessage]=useState("");const field=(key:keyof Settings,text:string)=>setValue(current=>({...current,[key]:text||null}));async function save(){setMessage("保存中…");const response=await fetch("/api/admin/site-settings",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(value)});const data=await response.json();setMessage(response.ok?"保存しました":data.error)}return <div className="space-y-4 rounded-2xl border border-slate-800 bg-slate-900/40 p-6"><Field label="サイト名" value={value.site_name} onChange={text=>field("site_name",text)}/><Field label="デフォルトtitle" value={value.default_title} onChange={text=>field("default_title",text)}/><label className="block text-sm text-slate-400">デフォルトdescription<textarea value={value.default_description} onChange={event=>field("default_description",event.target.value)} rows={4} className="mt-1 w-full rounded-lg border border-slate-700 bg-slate-950 p-3 text-white"/></label><Field label="CanonicalベースURL" value={value.canonical_base_url??""} placeholder="https://example.com" onChange={text=>field("canonical_base_url",text)}/><Field label="OGP画像URL" value={value.og_image_url??""} placeholder="https://example.com/og.png" onChange={text=>field("og_image_url",text)}/><label className="flex items-center gap-3 text-sm"><input type="checkbox" checked={value.robots_index} onChange={event=>setValue(current=>({...current,robots_index:event.target.checked}))}/>検索エンジンのindexを許可</label><button onClick={save} className="rounded-lg bg-violet-600 px-5 py-2.5 text-sm font-bold">SEO設定を保存</button>{message&&<p className="text-sm text-slate-400">{message}</p>}</div>}
function Field({label,value,onChange,placeholder}:{label:string;value:string;onChange:(value:string)=>void;placeholder?:string}){return <label className="block text-sm text-slate-400">{label}<input value={value} placeholder={placeholder} onChange={event=>onChange(event.target.value)} className="mt-1 h-11 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 text-white"/></label>}
