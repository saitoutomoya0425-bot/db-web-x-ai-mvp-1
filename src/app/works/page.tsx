import type { Metadata } from "next";
import { Breadcrumbs } from "@/components/breadcrumbs";
import { LoadMoreWorkGrid } from "@/components/load-more-work-grid";
import { getCatalogWorks,type CatalogSort } from "@/lib/queries/catalog";
export const metadata:Metadata={title:"作品一覧",description:"人気作品、新着作品、発売日順から作品を探せます。",alternates:{canonical:"/works"}};
function parseSort(value?:string):CatalogSort{if(value==="new"||value==="newest")return"newest";if(value==="release"||value==="recommended")return value;return"popular"}
export default async function WorksPage({searchParams}:{searchParams:Promise<{sort?:string;genre?:string;maker?:string;view?:string}>}){const params=await searchParams,size=1000;const sort=parseSort(params.sort),genre=params.genre?.slice(0,100)??"",maker=params.maker?.slice(0,100)??"";const works=await getCatalogWorks({limit:size,offset:0,sort,genre:genre||undefined,maker:maker||undefined});return <main className="mx-auto max-w-7xl px-5 py-12"><Breadcrumbs items={[{name:"トップ",href:"/"},{name:"作品一覧"}]}/><div className="mt-6 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-black">作品一覧</h1><p className="mt-2 text-sm text-slate-400">人気・新着・発売日から作品を探せます。</p></div><form className="flex gap-2"><input type="hidden" name="genre" value={genre}/><input type="hidden" name="maker" value={maker}/><select name="sort" defaultValue={sort} className="rounded-lg border border-slate-700 bg-slate-900 px-3"><option value="popular">人気順</option><option value="newest">新着順</option><option value="recommended">おすすめ順</option><option value="release">発売日順</option></select><button className="rounded-lg bg-violet-600 px-4 py-2 text-sm">適用</button></form></div><LoadMoreWorkGrid works={works} className="mt-8"/></main>}
