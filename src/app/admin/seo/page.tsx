import { redirect } from "next/navigation";
import { SeoSettingsForm } from "@/components/admin/seo-settings-form";
import { createClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function SeoPage(){const supabase=await createClient(),{data:{user}}=await supabase.auth.getUser();if(!user||user.app_metadata?.role!=="admin")redirect("/admin/login");const {data}=await supabase.from("site_settings").select("*").eq("id",true).maybeSingle();const initial=data??{site_name:"おかずDB",default_title:"おかずDB｜品番・女優・メーカーから作品検索",default_description:"品番から作品情報、女優、関連作品をすぐに確認",canonical_base_url:process.env.NEXT_PUBLIC_SITE_URL??null,og_image_url:null,robots_index:true};return <main className="mx-auto max-w-4xl px-5 py-8"><p className="text-xs font-bold tracking-widest text-violet-400">SYSTEM</p><h1 className="mt-2 text-3xl font-black">SEO設定</h1><p className="mt-2 text-sm text-slate-400">サイト共通のtitle、description、canonical、OGP、index方針を管理します。</p><div className="mt-8"><SeoSettingsForm initial={initial}/></div><section className="mt-6 rounded-xl border border-slate-800 p-5 text-sm text-slate-400"><p>サイトマップ：<code>/sitemap.xml</code></p><p className="mt-2">robots：<code>/robots.txt</code></p><p className="mt-2">構造化データ：作品・女優・パンくずへ実装済み</p></section></main>}
